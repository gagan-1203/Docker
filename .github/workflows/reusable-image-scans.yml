name: Reusable Container Image Security Scans

on:
  workflow_call:
    inputs:
      image_name_tag:
        description: 'The full name:tag of the container image to scan.'
        required: true
        type: string
    secrets:
      SNYK_TOKEN:
        description: 'Snyk API Token'
        required: true
      # --- NEW: Define inputs for Docker Credentials ---
      DOCKER_USERNAME: 
        description: 'Docker Registry Username'
        required: true
      DOCKER_PASSWORD: 
        description: 'Docker Registry Password/Token'
        required: true
        
jobs:
  
  # The login step must be added to all three parallel jobs
  
  trivy_scan:
    name: Trivy (Image & Config)
    runs-on: ubuntu-latest
    steps:
      # ... (existing checkout)
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          
      - name: Run Trivy Scan
        # ... (rest of the Trivy step)
        
  grype_scan:
    name: Grype (Vulnerability)
    runs-on: ubuntu-latest
    steps:
      # --- NEW: Login required for Grype as well ---
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          
      - name: Run Grype Scan
        # ... (rest of the Grype step)
        
  snyk_scan:
    name: Snyk Container
    runs-on: ubuntu-latest
    steps:
      # --- NEW: Login required for Snyk as well ---
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          
      - name: Run Snyk Container Scan
        # ... (rest of the Snyk step)
